# =====================================================
# NewPrint3D - Variáveis de Ambiente
# =====================================================
# Copie este arquivo para .env.local e preencha os valores

# =====================================================
# GERAL
# =====================================================

# URL pública do site (usado para SEO, canonical URLs, etc)
# Em desenvolvimento: http://localhost:3000
# Em produção: https://newprint3d.com
NEXT_PUBLIC_SITE_URL=https://www.newprint3d.com

# Ambiente (development, production, test)
NODE_ENV=desenvolvimento

# =====================================================
# BANCO DE DADOS NEON (PostgreSQL)
# =====================================================

# String de conexão do Neon PostgreSQL
# Formato: postgresql://[user]:[password]@[host]/[database]?sslmode=require
#
# Como obter:
# 1. Crie uma conta em https://neon.tech
# 2. Crie um novo projeto PostgreSQL
# 3. Copie a "string de conexão" com ?sslmode=require
#
# IMPORTANTE: Se deixar vazio, o sistema funcionará em modo demonstração
# usando dados locais (data/products.json)
DATABASE_URL=

# =====================================================
# AUTENTICAÇÃO JWT
# =====================================================

# Chave secreta para JWT (JSON Web Tokens)
#
# Como gerar uma chave segura:
# - Executar: openssl rand -base64 32
# - Ou uso: https://generate-secret.vercel.app/32
#
# IMPORTANTE: OBRIGATÓRIO em produção. Mínimo 32 caracteres aleatórios.
JWT_SECRET=

# =====================================================
# STRIPE (Pagamentos)
# =====================================================

# Chave secreta do Stripe (server-side)
# Encontre em: https://dashboard.stripe.com/apikeys
# Formato: sk_test_... (teste) ou sk_live_... (produção)
STRIPE_SECRET_KEY=

# Chave pública do Stripe (client-side)
# Formato: pk_test_... (teste) ou pk_live_... (produção)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=

# Webhook secret do Stripe
# Como obter:
# 1. Acesse https://dashboard.stripe.com/webhooks
# 2. Crie um novo endpoint: https://seu-dominio.com/api/webhooks/stripe
# 3. Selecione eventos: payment_intent.succeeded, payment_intent.payment_failed
# 4. Copie o "Segredo de assinatura" (whsec_...)
STRIPE_WEBHOOK_SECRET=

# =====================================================
# PAYPAL (Pagamentos Alternativos)
# =====================================================
# INSTRUÇÕES: Veja PAYPAL-SETUP.md para configurar
#
# Como obter credenciais:
# 1. Acesse: https://developer.paypal.com/dashboard/
# 2. Crie um aplicativo (ou use existente)
# 3. Copie Client ID e Secret
#
# IMPORTANTE: Configure AMBAS as variáveis abaixo:
# - PAYPAL_CLIENT_ID (server-side, preferencial)
# - NEXT_PUBLIC_PAYPAL_CLIENT_ID (client-side, fallback)
# - PAYPAL_CLIENT_SECRET (server-side apenas)

# Modo SANDBOX (desenvolvimento/testes):
# PAYPAL_CLIENT_ID=sandbox_client_id
# NEXT_PUBLIC_PAYPAL_CLIENT_ID=sandbox_client_id
# PAYPAL_CLIENT_SECRET=sandbox_secret

# Modo LIVE (produção):
# PAYPAL_CLIENT_ID=live_client_id
# NEXT_PUBLIC_PAYPAL_CLIENT_ID=live_client_id
# PAYPAL_CLIENT_SECRET=live_secret

PAYPAL_CLIENT_ID=
NEXT_PUBLIC_PAYPAL_CLIENT_ID=
PAYPAL_CLIENT_SECRET=

# =====================================================
# EMAIL (Opcional - Para notificações futuras)
# =====================================================

# Configuração SMTP para envio de emails
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USER=seu-email@gmail.com
# EMAIL_PASS=sua-senha-app
# EMAIL_FROM=noreply@newprint3d.com

# =====================================================
# ANALYTICS (Opcional)
# =====================================================

# Google Analytics
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# =====================================================
# LOGS E DEPURAÇÃO
# =====================================================

# Nível de log (error, warn, info, debug)
# LOG_LEVEL=informação

# =====================================================
# NOTAS IMPORTANTES
# =====================================================

# 1. NUNCA commite o arquivo .env.local para o repositório
# 2. Em produção (Vercel), configure estas variáveis no painel de Settings > Environment Variables
# 3. Para modo demonstração, deixe DATABASE_URL, JWT_SECRET e chaves do Stripe vazias
# 4. Para produção completa, TODAS as variáveis relacionadas a Stripe e DATABASE devem ser configuradas
